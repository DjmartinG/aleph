# Historial de aprendizajes (archivo)

> Entradas archivadas del «Registro de aprendizajes» de `CLAUDE.md` para aligerarlo
> (depuración del repo, jun-2026). **NO se borraron: se archivaron.** Son aprendizajes de la etapa
> Streamlit (2026-05/06) y sub-pasos de la migración ya superados por entradas más recientes.
> Las cifras doradas y los **gotchas operativos vigentes** siguen en `CLAUDE.md`.

## Registro archivado (más reciente primero)

- **2026-06-12 — PROMPT 4 · Fase 4b PREPARADA (no ejecutada): migración de datos autocontenida (sin depender del MCP):** El MCP de Supabase de Claude **sigue sin permiso** sobre la BD de la app (`jehkdhmngxvuvhxuhlan`, en otra organización). En vez de quedar bloqueados, se preparó una migración **que no necesita el MCP**: `db/migrations/0001_aleph_schema.sql` (modelo §4.3: companies/projects/scenarios/results_cache/actuals_*/audit_log; **idempotente, ADITIVO — no toca `proyectos`**; RLS deny-by-default que la `service_role` bypasea, así que no rompe la app; `fase` CHECK = config.ESTADOS; partial-unique de un solo `baseline` por proyecto) + `db/etl_import_v1.py` (lee `proyectos` con la `service_role`, valida con `schema.parse`, importa cada uno como `project` + `scenario v1 approved` con snapshot **bit a bit** + `results_cache` recalculado con el MISMO motor → cifras imposibles de mover; idempotente; imprime TIR/VPN/crédito como gate dorado). **Dos caminos de ejecución** (`db/README.md`): **A (recomendado)** Martin pega el SQL en el SQL Editor de su Supabase + corre `python db/etl_import_v1.py` (usa la `service_role` de secrets) — **no requiere conectar nada a Claude**; **B** Martin conecta la BD al MCP y el agente usa `apply_migration`. **Por qué importa:** cuando una integración (MCP) no alcanza un recurso, un **script determinista con la credencial que la app ya tiene** (service_role) es un camino self-contained que desbloquea sin fricción de configuración. **Pendiente:** ejecutar 4b (Martin) + el cut-over de `storage.py`/`repo.py` a leer de `scenarios` (fase posterior, más delicada) + 4d deploy del API.
- **2026-06-12 — PROMPT 3 · `checks.py` (cuadres) + cierre de `metrics.py`/`checks.py`:** Se añadió `engine/aleph_engine/checks.py`: **reconciliaciones** sobre el resultado de `calcular()` (no fórmulas nuevas), con 5 invariantes **verificados numéricamente** en los 3 proyectos reales — (1) `total_ingresos == ventas + recon`, (2) `sum(apalancamiento.ingresos) == total_ingresos` (recaudo cuadra), (3) `apalancamiento.acumulado[-1] == util_oper` (el flujo operativo cierra en la utilidad), (4) `pyg.cg + pyg.socio == pyg.resultados` (reparto), (5) `0 ≤ cap_credito ≤ valor_financiable` — más `check_spi` (rango plausible 0.4–2.0, para EVM). `test_checks.py` corre `correr(R)` sobre los 6 snapshots y exige que **todos los cuadres pasen** (engine **22 verdes**). Con esto PROMPT 3 ya tiene `metrics.py` (indicadores con etiqueta de base) **y** `checks.py`. **Confirmaciones de estado:** los 3 commits "sin subir" (mover motor / conectar Streamlit) **ya estaban en GitHub** (origin/main == HEAD `626858a`); y el gotcha de redeploy ("forzar el pull, no solo reiniciar") **ya está blindado** en `deploy_streamlit.*` por usar tag=SHA único + `container set` + restart. **Pendiente PROMPT 3 (último tramo):** desacoplar los **7 puntos de `app.py`** (§3.2 paso 13: borrar `_irr_anual` dup → `finanzas`; mover `consolidado`/`puntos_portafolio`/`pipeline_datos`/heatmap al motor; reconciliar el EVM real de Navarra con `evm.py`) — toca `app.py` (más delicado, con dorado + 73 verdes como red).
- **2026-06-12 — Reconciliar el plan Fase A–D con la realidad (ya hecho ~90%) + cerrar huecos: OneDrive, rename a `aleph`, `/docs`, `metrics.py`:** Llegó (otra vez) el plan original de 4 fases (andamiaje monorepo / snapshot dorado / extraer motor / sync de vuelta) como "hagámoslo de una vez". **Casi todo ya estaba hecho** en sesiones previas (PROMPT 2–3): NO se re-ejecutó a ciegas (un `git init`/repo nuevo habría borrado los 6 commits). Se **reconcilió** y se cerraron los huecos reales: (1) **OneDrive (la alerta vigente):** el repo de trabajo `C:\Code\aleph` está **fuera** de OneDrive (verificado: no cae bajo `$env:OneDrive`); el archivo que se corrompía era la **copia VIEJA** `…\OneDrive…\CODE MGG\app_factibilidad\.git` → **borrada** tras confirmar que es redundante (working tree limpio, su HEAD `e113c82` ya existe como ancestro aquí, y los datos local-only —proyectos_privados, 3 snapshots REALES, secrets— ya están en `C:\Code\aleph`). (2) **Repo renombrado** `cg-factibilidad-app` → **`aleph`** con `gh repo rename` (conserva historia + redirects) + `git remote set-url`. **GOTCHA clave:** los nombres de **Azure** (`cg-factibilidad-app` App Service, `cgfactibilidadacr`, `*.azurewebsites.net`) son **independientes del nombre del repo GitHub** — renombrar el repo NO toca el deploy (que además ya usa contexto local, no GitHub-source). (3) **Versión** del motor: se mantiene **2.39.0** (heredada), no 0.1.0 (decisión con Martin → ADR-0004). (4) **`engine/aleph_engine/metrics.py`**: diccionario único de indicadores (clave, nombre, **etiqueta de base**, definición, unidad, ruta en el resultado de `calcular()`) — **registro/lookup, sin cálculos**; las etiquetas replican las de `app.py` (confirmado: `tir_equity` = **"TIR socio CG"**, `tir_proyecto` = "TIR proyecto", `tir_apalancada_ref` = "TIR apalancada (ref)", `vpn_proyecto` = "VPN @TIO") + `test_metrics.py` (cada ruta resuelve; ninguna cifra sin etiqueta de base; doble TIR distinta). Engine **15 verdes**. (5) **`/docs` con 5 ADRs** (estrangulamiento, monorepo-un-repo, snapshot dorado, versión heredada, deploy SHA+bundle). **Por qué importa:** cuando un plan "nuevo" contradice lo ya construido, **reconciliar > re-ejecutar** (evita perder historia/duplicar); y un rename de repo GitHub es seguro porque los recursos de Azure no dependen de él. **Hueco menor consciente:** el plan pedía copiar `test_anclas/finanzas/schema` + `proyectos/` **dentro de `/engine`** para hacerlo standalone; se dejó el harness dorado leyendo desde `app_streamlit/tests/golden` (fuente única) — el engine ya valida las MISMAS cifras. Replicable si se quiere el engine 100% autónomo.
- **2026-06-12 — PROMPT 3 · bloque 2: `app_streamlit` consume `aleph_engine`; `cg_engine` ELIMINADO; imagen bundlea el motor:** Se repuntaron los **14 imports** (`from cg_engine import` → `from aleph_engine import`: app.py, ui/nav.py, execution/snapshot_dorado.py, 5 tests) —renombrado mecánico, la API pública es idéntica—, se **borró `app_streamlit/cg_engine`** (cero fórmulas financieras ya en la app: cumple la constitución) y `app_streamlit/pyproject.toml` quedó como **app** (solo pytest/ruff, sin empaquetar el motor). **Cambio de deploy (lo más delicado):** la imagen ahora **bundlea el motor** → el **contexto de build pasó de `app_streamlit/` a la RAÍZ del monorepo** y el Dockerfile se movió a **`Dockerfile.streamlit`** en la raíz (`COPY engine/ ./engine/ && pip install ./engine` + `COPY app_streamlit/ ./`). `deploy_streamlit.sh/.ps1`: contexto `.`, `--file Dockerfile.streamlit`, versión leída de `engine/aleph_engine/__init__.py`, y **captura de la imagen previa para ROLLBACK** si el health-check falla. `.dockerignore` reescrito a la raíz (con `**/` para excluir `tests`, `secrets.toml`, `proyectos_privados`, `*_REAL*.json`, `web`, `api`). **De-riesgo SIN Docker local** (no hay docker en la PC): el `pip install ./engine` del Dockerfile se validó construyendo el **wheel local** (`pip wheel ./engine` → `aleph_engine-2.39.0-py3-none-any.whl` OK) + import smoke + **app 73 verdes (con aleph_engine) + engine 12 verdes** + compileall + ruff. **OJO: NO se ha desplegado** — producción sigue en la imagen vieja (cg_engine) hasta que Martin corra el deploy y verifique health + pie «Aplicativo v2.39.0» (rollback listo si falla). `dev.sh`/`test.sh` (+.ps1) ahora **auto-instalan el motor** (`pip install -e ./engine`) si falta; el **CI** instala el engine, corre **sus tests** (harness dorado) y quitó `cg_engine` del `compileall`. `.gitignore` raíz: +`.tmp/`. **Por qué importa:** queda **un solo motor** (`aleph_engine`), la app es su cliente, y el contenedor es autocontenido. **Pendiente bloque 3:** extraer los 7 puntos acoplados de `app.py` (§3.2) + `metrics.py` (indicadores con etiqueta de base) + `checks.py`; y **el primer deploy real con el contexto nuevo** (verificar que la imagen monorepo arranca en Azure).
- **2026-06-12 — PROMPT 3 · bloque 1: mover el motor `cg_engine` → `aleph_engine` (harness dorado VERDE):** Se trajo la lógica financiera **tal cual** a `engine/aleph_engine` (11 módulos: config/errors/schema/finanzas/curvas/flujo/portafolio/ingresos/modelo/apalancamiento/evm), versión **heredada 2.39.0**. Verificación previa que lo hizo seguro: **todos los imports de `cg_engine` son relativos** (`from . import …`) o stdlib/terceros (scipy con fallback, dateutil, pydantic) y **no hay I/O de archivos ni imports de Streamlit/pandas/numpy** → copiar verbatim resuelve los imports dentro de `aleph_engine` sin tocar una línea. El **diseño `skipif` del esqueleto pagó**: al existir `aleph_engine.calcular`, el harness dorado se autoactivó → **12 passed** (6 dorado + 6 contrato; en CI 6, los REALES gitignored). `app_streamlit` **73 verdes** e intacto. **DECISIÓN clave (corrige el "move+shim" que se había planteado):** se hizo **COPIA** (no `git mv` + shim), dejando `cg_engine` vivo, porque el **contexto Docker de `app_streamlit` es solo `app_streamlit/`** → un shim que importara `aleph_engine` **rompería PRODUCCIÓN** (el paquete no está en la imagen). Conviven temporalmente **dos copias** del motor, **guardadas por el dorado en ambos lados**. **Por qué importa:** el bloque 1 prueba que `aleph_engine` reproduce las cifras congeladas (hito real, bajo riesgo, app desplegable sin cambios). **Bloque 2 (pendiente):** bundle de `aleph_engine` en la imagen Docker + repuntar imports de `app_streamlit` + **eliminar la copia `cg_engine`**; luego los 7 puntos de `app.py` (§3.2) + `metrics.py` (indicadores con etiqueta de base) + `checks.py`. Añadidas deps `scipy`/`python-dateutil` a `engine/pyproject.toml`.
- **2026-06-12 — PROMPT 2.3+2.4: esqueleto `aleph_engine` + scripts dev/test/deploy (cierre de Fase 2):** Se montó `/engine` como PAQUETE PURO `aleph_engine` (pyproject con versión única vía `__version__`, arranca en **0.1.0**), pero con **SOLO el contrato de datos** (`models.py`, portado tal cual de `cg_engine/schema.py`; `config.py` REPLICA los `ESTADOS` — el engine NO importa de `cg_engine`). La **lógica financiera NO se tocó**: se extrae en PROMPT 3. Clave del harness dorado del engine (`engine/tests/`): **lee los snapshots desde `app_streamlit/tests/golden/`** (fuente única, no se duplican — así los `*_REAL_*` gitignored no se filtran) resolviendo la raíz del monorepo, y el test de recálculo **se SALTA con `skipif` mientras `aleph_engine.calcular` no exista** → se autoactiva en PROMPT 3 sin tocar nada. Hoy: engine **6 passed** (el contrato parsea los 3 ilustrativos + 3 reales) **+ 6 skipped**; en CI 3+3 (REALES gitignored). `app_streamlit` sigue **73 verdes**. **Scripts en la RAÍZ** (`dev.sh`/`test.sh`/`deploy_streamlit.sh`) **+ equivalentes `.ps1`** (Martin usa PowerShell/Windows y no es dev → no depender de Git Bash). **deploy con tag=SHA:** elegí **build de contexto LOCAL** (`az acr build --registry cgfactibilidadacr --image cgapp:$SHA app_streamlit`) **gateado en working-tree LIMPIA**, en vez del build desde GitHub (`#main:app_streamlit`) que mencionaba el aprendizaje previo: el gate de árbol limpio garantiza que la imagen == el commit del tag SIN depender de `git push` (más simple y robusto para un no-dev). Cada deploy usa un **tag ÚNICO** (no `:latest`) → App Service SIEMPRE re-baja la imagen, lo que **esquiva de raíz el gotcha del digest cacheado**. Gotchas resueltos: (1) `--container-image-name` exige el **login server COMPLETO** `cgfactibilidadacr.azurecr.io/cgapp:$SHA` (no el nombre corto del registry); (2) `.gitattributes` con `*.sh text eol=lf` (CRLF rompe el shebang en Linux/CI/Git Bash: `bad interpreter ...^M`) + `git update-index --chmod=+x` (Windows git NO infiere el bit ejecutable); (3) verificación = poll a `/_stcore/health` (el HTML de Streamlit es client-side, el pie "Aplicativo vX.Y.Z" NO sale en `curl`). **Por qué importa:** cierra el PROMPT 2 (Streamlit intacto, snapshots existen y el harness los lee, Martin puede correr `test.sh`/`dev.sh` solo); el siguiente paso es **PROMPT 3** (extraer `cg_engine`→`aleph_engine` bloque a bloque, harness dorado verde tras cada uno). **Sigue pendiente:** rotar `SUPABASE_KEY` (service_role expuesta).
- **2026-06-11 — Captura de datos = pestaña ADMIN-ONLY ("Ingreso de datos"); y la "cabida" HOY es decorativa (hallazgos de exploración multi-agente):** El usuario pidió una pestaña independiente **solo para los 2 admins** como único punto de input, con todos los datos en orden **empezando por la cabida urbanística del lote**. La exploración del código (4 agentes) reveló: (1) **la cabida es decorativa** — las áreas (`lote_bruta/util`, `m2_vendibles/construidos`) solo alimentan KPIs descriptivos en "Urbanístico"; el motor financiero NO las usa, el driver real es `etapas[].und × precio → ventas → P&G/flujo/TIR`. No existe estudio de cabida POT (índices/altura/aislamientos/cesiones). Ojo: `lote_bruto_miles` es un **valor monetario** (costo del lote), NO un área. (2) **Admin-only por persona EXIGE SSO**: hoy solo hay roles `editor`/`viewer` (no `admin`); el camino de contraseña es **anónimo** (clave compartida), la única identidad por-persona es el email del login Microsoft (`st.session_state['_ms_user']`, disponible gracias a Easy Auth) → solución: lista `ADMINS` de correos en secretos + helper `es_admin()` junto a `gate()`. (3) **Input disperso en 3 lugares** ("Datos del proyecto" + islas "Distribución de costos" y "Costo de capital"), **sin `st.form`** (todo muta `par` in-place) y **una sola** persistencia (botón que hace upsert del JSON entero a Supabase) → migrar = reubicar widgets, el guardado no cambia. (4) **Gate temporal**: sin `fecha_inicio` en la etapa raíz el motor apaga recaudo/TIR/VPN/EVM. (5) **Hueco**: un proyecto **nuevo no se puede guardar** en la nube hoy. **Decisiones del usuario:** acceso por **email/SSO**; cabida **reorganizar ahora** (cero riesgo) + **calculadora POT después** (paso dedicado, con red de pruebas, capacidad nueva del motor). **Por qué importa:** re-escopó la **Fase 2 Paso 1** en 1a (andamiaje + `es_admin()`), 1b (módulo Ingreso admin, validar con `schema.parse`, tapar hueco proyecto nuevo, consumo→solo-lectura), 1c (menú 19→14 + ciclo de vida), 1d (capítulos). Todo en `app_factibilidad/NORTE_TABLEROS.md` (§1b). **Admins definidos** (lista `ADMINS`): mgomez@cgconstructora.com (Martin Gómez G) y jgonzalez@cgconstructora.com (José Alfonso González).
- **2026-06-11 — El ESTADO del proyecto es el eje rector de la app (ciclo de vida, Fase 2 Paso 0):** Con el experto se definió que cada proyecto lleva un campo `estado` con 4 etapas de pipeline (`prefactibilidad → aprobado → construccion → entregado`) y que la UI se **adapta** al estado: pre-fact = decisión ir/no-ir (lote = SUPUESTO, sin Seguimiento, gate multicriterio), construcción/entregado = monitoreo plan vs real. Implementado como **dato** primero (lo más seguro): `config.ESTADOS`/etiquetas/umbrales de aprobación (TIR equity + VPN>0 a TIO + margen, provisionales), `Meta.estado` validado en `schema.py` (rechaza estados mal escritos, fuente única = `config.ESTADOS`), e inserción **quirúrgica de texto** del campo en los 6 JSON (sin reformatear → diff limpio en los públicos). NO toca el cálculo: 34 tests verdes. Estados reales hoy: Navarra/Dominica = construcción; Torres de Campiñas = **aprobado** (obra inicia 2026-07-15). La herramienta estrella de pre-fact será el **lote breakeven** (precio máx pagable para rendir el umbral) + evaluar un precio. **Por qué importa:** (1) el norte de tableros (`app_factibilidad/NORTE_TABLEROS.md` §0) gira en torno a esto — pipeline/embudo, filtro por estado, secciones que aparecen/ocultan según estado; (2) agregar un eje conceptual nuevo como CAMPO validado + tests, antes de tocar UI, mantiene el riesgo en cero. **Pendiente Fase 2:** andamiaje modular (`ui/`), reorganizar menú 19→14, Pipeline/Embudo, Decisión de inversión, Comparar candidatos.
- **2026-06-11 — Reestructuración del motor con "golden tests primero" (Fase 1, sin romper nada):** Se refactorizó `engine/` → paquete **`cg_engine`** en 6 pasos atómicos (paquete instalable; TIR/VPN/WACC única en `finanzas.py` sin ciclo de import; `config.py` sin números mágicos; `errors.py` + logging en vez de swallows silenciosos; `schema.py` Pydantic validable en el borde; `flujo.py` helper compartido; versión única `cg_engine.__version__`) **sin cambiar una sola cifra auditada**. La clave: una **suite de anclas/golden tests** (Fase 0: `tests/test_anclas.py` + CI en GitHub Actions) que clava UO/TIR/VPN de los 3 proyectos reales ANTES de tocar el motor; cada paso se verifica contra ella + ruff + `compileall` + PR con CI verde, y se revierte si una cifra se mueve. **Juicio de experto sobre el consejo genérico (un diagnóstico automático sugirió ambas cosas, AMBAS habrían roto algo):** (1) la fecha de corte del EVM (`2026-05-01`) **NO** se cambia a `date.today()` — es el corte de los datos de comité, cambiarla daría un SPI falso; (2) `flujo_caja` (PERT, vista simple) y `flujo_apalancado` (Gauss, waterfall auditado que produce las anclas) **NO** se fusionan — son modelos intencionalmente distintos. **Por qué importa:** la red de pruebas convierte una refactorización riesgosa en segura y repetible (PR pequeño por paso, CI verde, merge con rebase); y el conocimiento del DOMINIO gana sobre el "best practice" genérico. Plan completo en `app_factibilidad/REESTRUCTURACION.md`. **Pendiente:** Fases 2-5 (UI modular, modelo de datos, puertos ERP/CRM, CI/CD+rotar secretos). El contenedor desplegado sigue en v2.35.0 — al desplegar la Fase 1 hay que reconstruir la imagen (`az acr build`).
- **2026-06-11 — Azure App Service: ir directo a CONTENEDOR Docker, NO al auto-build (Oryx):** Desplegar la app Streamlit en App Service Linux modo "Code" (Oryx) fue un infierno de horas: el build daba "successful" pero el runtime moría con `No module named streamlit` (el venv `antenv` del build NO se encuentra en runtime; `WEBSITE_RUN_FROM_PACKAGE` rompe el build de Python dejando wwwroot read-only; un Startup Command custom corre con el python del SISTEMA sin activar antenv). **Solución definitiva:** empaquetar en un **contenedor** (`Dockerfile` con `FROM python:3.12-slim` + `pip install -r requirements.txt` + `CMD streamlit run`) y construirlo con **`az acr build`** a un Azure Container Registry (build en la nube, sin Docker local), luego App Service apuntando a esa imagen (`az webapp config container set`, `WEBSITES_PORT=8000`, websockets+alwaysOn ON). Las librerías quedan horneadas: sin Oryx, sin venv que buscar. Además la instancia original `cg-factibilidad` quedó **corrupta** tras decenas de cambios (504 hasta con `python -m http.server`) → hubo que **recrear limpia** (`cg-factibilidad-app`, mismo plan B1, copiando los secrets de la vieja con `appsettings list ... -o json > s.json` + `set --settings @s.json`). URL nueva: `https://cg-factibilidad-app.azurewebsites.net`. **Por qué importa:** (1) para Streamlit/Python en Azure, ir derecho a contenedor evita TODA la clase de problemas de Oryx; (2) los logs de App Service Linux se atrasan/no refrescan (inútiles para depurar arranque) — el contenedor da control total; (3) si una instancia se "envenena" de tanto cambio, recrear es más rápido que depurar; (4) `--admin-enabled true` en el ACR + pasar user/pass al webapp es la vía simple de auth. **Estado (2026-06-11):** Easy Auth ✅, instancia vieja `cg-factibilidad` **borrada** ✅, contenedor **reconstruido a v2.38.0** desde GitHub `main` ✅ (`az acr build --registry cgfactibilidadacr --image cgapp:latest https://github.com/DjmartinG/cg-factibilidad-app.git` + `az webapp restart`; incluye Fase 1 + ciclo de vida + Ingreso admin + Monte Carlo). Lista `ADMINS` definida en Azure (mgomez@/jgonzalez@) → admin por email funciona en prod. Recursos reales: grupo `Cg-factibilidad`, plan `ASP-Cgfactibilidad-b208` (B1, 1 solo plan), registry `cgfactibilidadacr`, webapp `cg-factibilidad-app`. **GOTCHA de redespliegue (importante):** tras `az acr build ... cgapp:latest`, App Service **NO re-baja** la imagen `:latest` con un simple `az webapp restart` (cachea el digest viejo → la app sigue en la versión anterior). Hay que **forzar el pull** con `az webapp config container set -g Cg-factibilidad -n cg-factibilidad-app --container-image-name cgfactibilidadacr.azurecr.io/cgapp:latest --container-registry-url https://cgfactibilidadacr.azurecr.io` (mantiene las credenciales del registry, que viven como app settings aparte) y **luego** `az webapp restart`. Verificar por el pie `Aplicativo vX.Y.Z`. **Pendiente:** rotar `SUPABASE_KEY` (service_role expuesta en sesiones pasadas).
- **2026-06-08 — Probar la app con AppTest sin quedar atrapado en el candado:** `gate()` hace `st.stop()` en la pantalla de login cuando `secrets.toml` tiene `CLAVE_EQUIPO` (caso local real). En `AppTest` eso hace que NINGUNA sección renderice (0 excepciones, pero contenido vacío → falso positivo). Saltar con `at.session_state['_rol']='editor'` ANTES de `at.run()`, y forzar la sección monkeypatcheando `streamlit_option_menu.option_menu=lambda *a,**k: '<Sección>'` antes de `AppTest.from_file`. **Por qué importa:** sin esto las pruebas validan el login, no la lógica; siempre setear rol y sección para probar de verdad.
- **2026-05-28 — App de factibilidad (v1.0.0):** Aplicativo Streamlit multi-proyecto en `app_factibilidad/` (repo Git, tag v1.0.0). Motor en `engine/` (curvas PERT + modelo: P&G, reparto, flujo, escenarios). Proyecto Dominica validado al 99,87%. **Por qué importa:** es la herramienta operativa; agregar proyectos = un JSON en `proyectos/`.
- **2026-05-28 — Fuente única de verdad:** toda la lógica financiera vive en `app_factibilidad/engine/` (Python); la UI (Streamlit) solo presenta. NO duplicar fórmulas en JS/Excel. **Por qué importa:** consistencia y auditabilidad (estándar FAST de modelación).
- **2026-05-28 — Enfoque híbrido de factibilidad:** el modelo propio posee P&G/costos/crédito (crédito constructor calibrado = $29.601M, exacto al aprobado); la TIR APALANCADA de decisión (21,83%) se toma del modelo aprobado. **Por qué importa:** el revolving del crédito propio aún no replica el waterfall de fiducia; no comparar TIR no-apalancada vs apalancada.
- **2026-05-28 — Leer .xlsx bloqueados / OneDrive solo-en-nube:** si Excel tiene el archivo abierto o OneDrive lo marca como placeholder, copiar primero con `Copy-Item` y leer la copia. **Por qué importa:** evita PermissionError y "file does not exist" al leer con openpyxl.
