# web — Next.js (App Router) + TypeScript + Tailwind + shadcn/ui

UI profesional de ALEPH (nivel Linear/Mercury/Tremor). **SOLO presenta** — ningún cálculo financiero en
el frontend. Consume `/api` (cliente tipado generado desde el OpenAPI). Auth: **NextAuth + Entra ID**.

> **Estado:** esqueleto. Se construye desde **PROMPT 5** (design tokens + componentes canon ANTES de páginas).
> Deploy: Vercel o Azure SWA (a confirmar). Requiere Node.js 20+.
